# This workflow runs on every push and pull request, using Taskfile targets
# to lint, compile, and test both the tomc compiler and the VS Code extension.

name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

permissions:
  contents: read

jobs:
  tomc:
    name: Lint, compile and test (tomc)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt, clippy

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            tomc/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('tomc/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Lint
        run: task lint

      - name: Compile
        run: task build:debug

      - name: Test
        run: task test

  vscode-extension:
    name: Compile TypeScript VSCode extension
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: vscode-extensions/tomi-language-support/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('vscode-extensions/tomi-language-support/package-lock.json') }}
          restore-keys: ${{ runner.os }}-npm-

      - name: Compile
        run: task build:vscode

